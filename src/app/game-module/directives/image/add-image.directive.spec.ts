import { AddImageDirective } from './add-image.directive';

describe('AddImageDirective', () => {
  it('should create an instance', () => {
    const directive = new AddImageDirective();
    expect(directive).toBeTruthy();
  });
});
